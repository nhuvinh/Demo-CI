Create a sample Node.js Rest API that allows users to perform CRUD (Create, Read, Update, Delete) operations on a simple resource called "tasks". The API will use Express.js and a simple in-memory array to store tasks.

Then, create the test project using Jest to test the API endpoints.

After that, provide all test cases for the API endpoints. Make sure to generate the comprehensive test cases covering all CRUD operations and Test Reports.

After that, create the GitHub actions to automate the CI pipeline for the project, including building, testing, and reporting.

The pipeline is triggered on every push/merge to main and dev branches. 